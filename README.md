# Python_exercises
Simple python exercises

## Subheader 

Exercise 0: Git commands
- git status
- git clone
- git add: need to specify which files to add, or just use . to add all new files and modified files.
- git commit -m "message": commit and comment it
